# Dev-only tooling. Not installed into Home Assistant.
#   /opt/homebrew/bin/python3.12 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#
# bleak / bleak-retry-connector are pinned to the versions Home Assistant
# 2026.8.0 ships, so local tests exercise the same library behaviour.
requests>=2.31
bleak==3.0.2
bleak-retry-connector==4.6.3
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.6
websockets>=13.0
